Drop the login screen's hero photo here as:

    login-hero.jpg

Referenced by `.login-panel-image` in `app.css`. If the file is missing, that
panel falls back to the existing brand gradient (teal → gold) instead of a
broken-image icon — so the app still looks correct without it.
